- project:
    name: zuul/project-config
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop

- project:
    name: plugins/checks
    third-party-check:
      jobs:
        - system-config-build-image-gerrit-master
